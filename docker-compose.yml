version: '3.8'

services:
  bus-buddy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bus-buddy-dashboard
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
